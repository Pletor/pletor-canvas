apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: pletor
spec:
  selector:
    app: pletor
    component: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
